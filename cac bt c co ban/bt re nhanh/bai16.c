#include <stdio.h>
int main()
{
    int luonggio, giolv, tongluong;
    printf("Nhap so gio lam viec: ");
    scanf("%d", &giolv);
    printf("Nhap so luong moi gio lam viec: ");
    scanf("%d", &luonggio);
    tongluong = giolv * luonggio;
    if (giolv > 40)
    {
        tongluong = giolv *
    }
    return 0;
}